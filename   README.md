# 📌 NewsVerifyAI

Сервис для проверки утверждений по новостным источникам с использованием NER (spaCy) и AI-оценки (OpenAI / fallback). Поддерживает два стиля ответа: `simple` и `formal`.

## 🚀 Запуск (локально)

```bash
git clone <repo>
cd NewsVerifyAI

python -m venv venv
.env\Scripts\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```

## 📎 Файл .env

```
NEWSAPI_KEY=твоя_ключ
OPENAI_API_KEY=твой_ключ_или_оставь_пусто
DEBUG=True
```

## 🌐 Открыть в браузере

```
http://localhost:8000/ui
```

## 🐳 Запуск через Docker

```bash
docker build -t newsverifyai .
docker run -p 8000:8000 --env-file .env newsverifyai
```

## 🔍 Пример запроса

```bash
curl -X POST http://localhost:8000/check   -H "Content-Type: application/json"   -d '{"text":"Tesla запускает новый завод","style":"simple"}'
```

Или просто через UI по адресу `/ui`.

## 📦 Технологии

- Python, FastAPI  
- spaCy (NER)  
- NewsAPI  
- OpenAI API / fallback logics  
- SQLite (кэш)  
- Docker  

## 📌 Статусы ответа

| Статус | Значение |
|--------|----------|
| `confirmed` | Есть публикации |
| `not_found` | Не найдено, низкая вероятность |
| `uncertain` | Не найдено, возможное событие (при наличии AI) |

## 👨‍🎓 Для защиты

Система проверяет утверждения через новостные источники и, при необходимости, оценивает правдоподобность через AI-модель. Если модель недоступна — применяется fallback-логика. Проект модульный, кэшируется, поддерживает контейнеризацию.
